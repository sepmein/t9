KOKIYA.com nodejs server.
Host by joyent.

En:
Kokiya is a very simple nodejs microblog.
Usage:
1.Install NodeJs(with npm), 
2.Install Mongodb, 
3.run : mongod
4.download the project & cd to the path
5.run : npm install
6.run : node app.js

Check localhost:8000 and you'll get that.


Frameworks using: ExpressJs, Mongoose, Session-mongoose, BackboneJS, Twitter BootStrap, Html5Boilerplate, Jquery

中文：
kokiya是一个非常简单的微薄。
使用方法：
1、安装 NodeJs(并且安装npm)
2、安装 Mongodb
3、命令行： mongod（开启mongodb）
4、下载项目，cd至该项目
5、命令行：npm install
6、命令行：node app.js

打开浏览器输入localhost:8000。

所使用的框架：ExpressJs, Mongoose, Session-mongoose, BackboneJS, Twitter BootStrap, Html5Boilerplate, Jquery